# 4주차 자료 검증 기록

확인일: 2026-09-23.

## 2026-09-23 재구성

공개된 덱과 독립적으로 작성한 재구성 초안 B를 비교하여 11장·610초로 다시 편집했다.
「증명되지 않은 항목」 장을 빼고 그 범위를 임무 입력 구성 표와 향후 추진 계획에 반영했다.
카메라 교정은 설계 선택으로 설명하고, 3주차 피드백 답변은 카메라 장착 사례·설치 높이 조건·근거리 측정 한계의 세 장으로 나눴다.
제어 입력 후보는 기존 전장과 조작 축의 실물 사진 장의 원고와 요약 띠에 합쳤다.

Codex 적대적 검토 지적을 반영했다. ifm 높이 범위는 자사 센서 안내로만 표시하고 축척 적용이나 D435i 전용 값으로 전이하지 않았다.
근거리 거리 계산에는 포크 뿌리의 캐리지에 카메라를 정면 장착한다는 가정을 표시했다.
목적지는 시뮬레이터 좌표라고 명시했다. 삽입 중 간섭 검사는 근접 포켓 추적과 구분하고,
7.65 mm 는 계획 종점 대비 오차로 표기했다.

- 1920 × 1080 헤드리스 렌더로 11장 전부 확인했다. 본문 넘침은 없다(모든 장 scrollHeight = clientHeight).
  두 줄로 꺾이던 근거리 측정 한계·운반 임무 장의 수치 라벨을 줄였다.
- 운반 임무 장의 영상이 활성 슬라이드에서 자동 재생·반복하는 것을 확인했다.
- 원고 3,599자 / 610초 = 5.90자/초.

## 이전 공개본의 검증 기록 (재구성 전)

## 확인한 것

- `python3 build_deck.py`로 11장·610초(10분 10초) 생성. 장수와 시간 합계는 빌드에서 단언으로 고정했다.
- 1920 × 1080 헤드리스 렌더로 **11장 전부** 배치를 확인했다. 잘림·겹침·미렌더는 없다.
- 9쪽의 영상이 실제 프레임을 그리고 자동 재생·반복하는 것을 활성 슬라이드에서 확인했다.
- 사진은 원본에서 **EXIF 를 전부 제거**했고(태그 0개 확인), 다른 팀의 장비가 찍힌 배경은 잘라냈다.
  원본 11장 중 9장에 위도·경도 태그가 있었다. 원본 체크섬은 로봇 저장소의
  `docs/validation/2026-09-23-chassis-intake.md` §5 에 있다.
- 원고 분량 4,020자 / 610초 = **6.59자/초**. 3주차(3,956자 / 6.49자/초)와 맞췄다.
- 수치는 전부 [SOURCES.md](SOURCES.md)의 검증 기록·공식 문서에 대응한다. 출처 없는 수치는 없다.
- 각 쪽의 발표자 노트에 그 화면을 만든 것(실물 사진 · Isaac Sim · 측정값 도식 · 공식 사양 도식)을 적었고,
  실물 사진과 시뮬레이션이 섞이는 3·4·8·9쪽은 **화면 캡션에도** 표기했다.
- 당시 저장소 시험 통과: `python3 -m unittest discover -s tests` 9건, `node --test tests/*.test.cjs` 7건,
  주차 JS 문법 검사, `tools/build_site.py`. 현재 `week.json` 은 `published` 이다.

## 독립 검토 3건

작성자와 별도로 세 갈래의 검토를 받고 지적을 반영했다.

| 검토 | 반영한 것 |
|---|---|
| 수치·과장 대조 | 9쪽 영상이 실물 주행으로 들릴 수 있어 화면·원고에 **Isaac Sim** 명기. 8쪽 첫 문장을 "시뮬레이션 안의 카메라"로 한정. 2쪽 ①의 "평가 완료"를 **"평가 1회 · 재평가 예정"**으로 정정(반 화소 결함이 저장된 Gazebo 데이터에도 남아 있다). 9쪽 2 mm 검사의 방향을 **"2 mm 넘게 떨어져 있는지"**로 바로잡음(코드는 간격이 여유 아래로 내려가면 기각한다). 조종기 버튼 목록이 전수로 들리지 않게 한정. 12V 를 "표기"로 한정 |
| 2·3주차 대비 문체·구성 | 원고 4,349 → 4,020자로 줄여 밀도를 3주차에 맞춤. takeaway 를 원고 복사에서 **수치 압축형**으로 교체. "시뮬레이터 정답" 설명의 2·7·10쪽 3중 반복을 7쪽으로 통합. headline 3개를 명사형으로. 라벨을 2·3주차와 같은 **"향후 추진 계획"**으로 복원. 라이다 → **2D LiDAR**, 십이볼트 → **12볼트**, 디엘에스 → **DLS** 로 표기 통일. 규약 → **기준**, 포크 캐리지·정격·화소·계통 오류에 풀이 추가 |
| 1920 × 1080 렌더 | 제어기 라벨 사진이 232 px 로 표시돼 원고가 읽는 글자를 볼 수 없었다 → **라벨 클로즈업으로 교체**. 2·11쪽의 빈 공간(본문 띠의 50 % 이상)을 카드가 채우도록 수정. 11쪽 상태 라벨을 칸 바닥으로 정렬. 5쪽 후보 카드 19px → 22px, 캡션 17px → 19px, swap 라벨 18px → 20px. 5쪽 표 첫 열의 어절 중간 줄바꿈 해소 |

## 확인하지 않은 것

- **실물 성능.** 차체는 입고됐으나 치수·질량·정격을 하나도 재지 않았고 통전 시험도 하지 않았다.
- **실물 카메라.** D435i 를 장착하거나 교정하지 않았다. 8쪽은 Isaac 입력 경로의 검증이다.
- **여러 조건의 성공률.** 기준 수정이 검출 입력을 바꾸어 이전 성적은 무효이고 재실행하지 않았다.
- **발표장 환경에서의 재생.** 브라우저와 화면이 바뀌면 다시 확인한다.
- **5쪽의 산업 사례 원문 일부.** 페이월·403 으로 본문을 열지 못한 자료는 인용하지 않았다.
  인용한 것은 전부 원문을 직접 확인한 것이다.

## 남은 작업

- 발표 배정 시간이 10분 10초와 다르면 5쪽과 9쪽 순으로 줄인다.
- 발표장 브라우저에서 9쪽 영상 재생을 미리 확인한다.
- `week.json` 의 `status` 는 `published` 이다.
