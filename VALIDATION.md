# 발표자료 검증 기록

검증일: 2026-09-08. 대상: 이 폴더의 최종 웹 발표자료.

| 확인 항목 | 결과 |
|---|---|
| `build_deck.py` 실제 실행 | 12장 생성, 장별 시간 합계 600초, 발표 발화 원고 4,027자 |
| 1280 × 720 | 12장 전체 텍스트 잘림·요소 겹침 검사 통과 |
| 1920 × 1080 | 12장 전체 텍스트 잘림·요소 겹침 검사 통과 |
| 390 × 844 | 12장 전체 축소 레이아웃 검사 통과. 실제 휴대전화 터치 검증은 하지 않음 |
| 시각 검토 | 최종 12장을 1280 × 720 렌더로 각각 직접 확인 |
| 키보드 | →, End, Home, PageDown, PageUp, 0, R의 7개 이동 검증 통과 |
| 전체화면 | F로 진입·종료, 전체화면에서 썸네일 숨김 확인 |
| JavaScript | 실행 예외 0건, `support.js`와 `deck-stage.js`의 `node --check` 통과 |
| 이미지·글꼴 | 12장 정상 로드, 깨진 이미지 없음, 로컬 한국어 글꼴 로드 확인 |
| 외부 네트워크 의존 | 페이지 로드·슬라이드 순회 중 비로컬 HTTP 요청 0건 |
| 실행 스크립트 | `bash -n present.sh` 통과, 실제 HTTP 서버 실행 확인 |
| UOS·발표 런타임 | 참고 드론 레포의 12개 파일과 바이트 단위 동일 |

연결된 앱 브라우저가 없어 로컬 Chrome headless와 DevTools Protocol로 실제 웹 페이지를 실행했습니다. 기하 검사는 세 해상도에서 각각 12장, 총 36개 화면을 대상으로 했습니다. 모바일은 브라우저 화면 크기를 바꾼 검사이며 실제 기기 검증이 아닙니다.

초기 렌더에서 본문·그림 설명이 하단 요약 띠와 겹치는 문제가 있어 내용을 줄이고 그림 높이를 조정한 뒤 재검증했습니다. UOS 표지의 두 제목 행은 Noto 글꼴의 텍스트 범위 박스가 일부 겹친다고 탐지되었습니다. 실제 글자 사이의 여백을 렌더에서 확인했고, 원본 템플릿의 같은 제목 블록 내부 행 박스만 겹침 검사에서 제외했습니다. 서로 다른 본문·이미지·요약 띠의 겹침 검사는 유지했습니다.

이 검증은 발표자료의 생성·실행·화면 표시를 확인한 것입니다. 실제 10분 발화 리허설, 프로젝터, 실제 모바일 터치, 로봇 코드·센서·주행·삽입 성능은 검증하지 않았습니다. 이 표는 최초 로컬 제작 시점의 검증 기록이며, PPTX 변환은 수행하지 않았습니다.


## GitHub Pages 배포 구성 검증

2026-09-08, 별도 `forklift-presentations` 레포로 분리하면서 다음을 확인했습니다.

- 배포 빌더를 작성하기 전 세 배포 계약 테스트가 실제로 실패하는 것을 확인했습니다. 구현 후 세 테스트 모두 통과했습니다.
- 12장·600초, `/forklift-presentations/` 하위 경로에서 HTML과 CSS가 참조하는 모든 로컬 자산의 존재를 확인했습니다.
- 비어 있지 않은 배포 출력 폴더와 레포 루트를 출력 대상으로 지정하면 기존 파일을 보존하며 거부하는 것을 확인했습니다.
- 게시 묶음은 23개 파일이며, `.nojekyll`과 로컬 런타임·글꼴·이미지를 포함합니다. 빌더와 테스트·워크플로는 사이트 묶음에서 제외합니다.
- GitHub Actions는 커밋된 생성물과 재생성 결과의 일치, 위 세 테스트, JavaScript 문법 검사를 통과한 경우에 배포합니다.

최신 원격 배포 상태: https://github.com/lightminn/forklift-presentations/actions/workflows/presentation-pages.yml


## 예상 주행 파이프라인 추가 검증

2026-09-09, 기존 11쪽 ‘개발 추진 일정’ 다음에 ‘예상 주행 파이프라인’을 삽입하고 ‘향후 추진 계획’을 13쪽으로 이동했다. 새 장에 45초를 배정하여 총 13장·645초로 갱신했다.

- 생성기 실행으로 HTML·원고·메타데이터를 재생성했다.
- 배포 계약 테스트 3개와 런타임 JavaScript 문법 검사 2개가 통과했다.
- Chrome headless에서 1280 × 720, 1920 × 1080, 390 × 844의 13장씩 총 39개 화면을 순회했다. 검사한 본문·그림의 캔버스 이탈, 텍스트 블록의 가로 넘침, 본문 구역 간 겹침 및 새 도식의 컨테이너 이탈이 없었다. 카드 밖으로 이어지는 연결 화살표는 의도한 배치이므로 카드의 scrollWidth 판정에서 제외하고 내부 텍스트를 별도로 검사했다.
- 새 12장의 렌더를 직접 확인하고 하단 설명과 정지 조건 띠 사이 여백을 조정했다. 브라우저 실행 예외는 0건이었다.

이 기록은 로컬 웹 발표자료의 추가 장과 표시 상태에 대한 검증이다. 실제 로봇 성능과 발표 시간 리허설은 포함하지 않는다.

## A–D 접근 애니메이션 검증

2026-09-09, 7·8쪽의 정적 그림을 접근·정렬·삽입 동작 애니메이션으로 변경하였다. 발표 환경은 사용자 지정에 따라 1920 × 1080, 16:9 전체화면을 기준으로 확인하였다.

- 새 동작 검사 4개가 구현 전 실패하고 구현 후 통과하였다. 정적 그림 대체, 최종 삽입 위치, C·D의 후진 거리 차이와 D의 후방 장애물 간격, 이동·회전의 연속성을 검사한다. 이는 개념도 좌표의 검사이며 실제 차량의 운동 성능 검증이 아니다.
- 기존 배포 검사 3개와 JavaScript 문법 검사 3개가 통과하였다. 사이트 묶음 24개 파일에 새 `case-motion.js`가 포함된다.
- Chrome의 FHD 전체화면에서 7·8쪽을 직접 확인하였다. 본문 구역 간 겹침, 시나리오·제어 버튼·범례의 캔버스 이탈 및 가로 넘침이 없었다.
- 자동 재생, 일시정지, 처음부터 재생, 비활성 슬라이드 정지, C·D 후진 단계, 삽입 완료 위치, 슬라이드 재진입 시 초기화를 실제 브라우저에서 확인하였다. 재생 중 외부 자산 요청은 없었다.
- 운영체제의 동작 줄이기를 브라우저에서 모사하여 자동 재생 중지와 명시적 재생 버튼의 동작을 확인하였다.
- 사용자 지정 발표 환경에 따라 휴대전화 검사는 중단하였다. 13장·645초와 UOS 기본 캔버스 및 발표 런타임은 유지하였다.

## 원본 구글 슬라이드 요소를 사용한 애니메이션 수정

2026-09-09, 사용자 피드백에 따라 그려진 지게차 도식을 제거하고 구글 슬라이드의 개별 이미지 요소를 사용하였다.

- PPTX 내보내기의 지게차·팔레트 PNG를 재인코딩 없이 추출하였다. 두 파일의 SHA-256이 원본 이미지와 일치한다.
- 원문의 이미지 크기·자르기 범위·위치·회전값을 기록하고, A·B·C와 D의 두 대안에서 시작·중간·삽입 자세를 보존하였다. 좌측 및 후방의 ㄴ자 장애물도 두 대안에 각각 반영하였다.
- 원본 이미지 동일성, 두 D 대안과 장애물의 보존, 원문 자세의 일치, 후진 거리 및 장애물 간격, 자세 연결의 연속성을 확인하는 검사 5개가 통과하였다. 기존 사이트 묶음 검사 3개도 통과하였다.
- 1920 × 1080 전체화면에서 7·8쪽의 다섯 장면을 직접 확인하였다. 자동 재생·정지·재시작, 비활성 슬라이드 정지, C·D2의 후진과 D1의 회전, 삽입 완료 자세를 검사하였다. 본문 구역 간 겹침과 캔버스 이탈 및 가로 넘침은 없었다.
- CSS와 애니메이션 파일 URL에 내용 해시를 추가하여 변경 전 파일이 브라우저 캐시에 남는 경우를 방지하였다. 이미지 자체의 픽셀은 변경하지 않았다.

정지 자세 사이의 보간은 발표용 애니메이션이며 실제 차량의 계산된 주행 경로를 의미하지 않는다. D1의 회전 가능 여부는 원고에 기재한 대로 차체 선정 후 확인한다.
